<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="kash.triplog.Main.MainView"
             xmlns:ktm="clr-namespace:kash.triplog.Main"
             xmlns:vc="clr-namespace:kash.triplog.Infrastructure.Converters;assembly=kash.triplog.dll"
             Title="Trip Log">

  <ContentPage.Resources>
    <ResourceDictionary>
      <vc:ReverseBooleanConverter x:Key="not" />
    </ResourceDictionary>
  </ContentPage.Resources>
  
  <ContentPage.ToolbarItems>
      <ToolbarItem Name="NewButton" Text="New" Command="{Binding NewCommand}" />
  </ContentPage.ToolbarItems>

  <StackLayout x:Name="RootLayout" HorizontalOptions="Center">
    <ListView x:Name="Entries" ItemsSource="{Binding LogEntries}" IsVisible="{Binding IsBusy, Converter={StaticResource not}}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Title}" Detail="{Binding Notes}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <StackLayout x:Name="Loader" Padding="0,50,0,0" Orientation="Vertical" HorizontalOptions="Center" VerticalOptions="Center" IsVisible="{Binding IsBusy}">
      <ActivityIndicator IsRunning="true" />
      <Label Text="Cargando..." />
    </StackLayout>
  </StackLayout>
</ContentPage>